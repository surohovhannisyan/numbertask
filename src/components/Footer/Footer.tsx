import React from "react";

import styles from "./Footer.module.scss";

const Footer = () => (
  <div className={styles.footerWrapper}>
    <p className={styles.footerText}>Number Card Task</p>
  </div>
);

export default Footer;
